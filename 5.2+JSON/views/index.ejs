<!DOCTYPE html>
<html>
  <head>
    <title>Taco Recipes</title>
    <link rel="stylesheet" type="text/css" href="/styles/main.css" />
  </head>

  <body>
    <h1>Taco Town 🌮</h1>

    <form action="/recipe" method="POST" class="buttons">
      <button type="submit" value="chicken" name="choice">🍗</button>
      <button type="submit" value="beef" name="choice">🥩</button>
      <button type="submit" value="fish" name="choice">🐟</button>
    </form>
    <div id="recipeContainer" class="hidden">
      <!-- If no recipe yet, show: -->
      <% if (!locals.userTaco) { %>
      <h2>Pick your favourite taco ingredient 👆</h2>
      <% } else { %>
      <h2><%=userTaco.name%></h2>
      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li>
          <%= userTaco.ingredients.protein.name %>, <%=
          userTaco.ingredients.protein.preparation %>
        </li>
        <li><%= userTaco.ingredients.salsa.name %></li>
        <% for (let i = 0; i < userTaco.ingredients.toppings.length; i++) { %>
        <li>
          <%=userTaco.ingredients.toppings[i].quantity %> of <%=
          userTaco.ingredients.toppings[i].name %>
        </li>
        <% } %>
      </ul>
      <% } %>
      <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->
    </div>
  </body>
</html>
